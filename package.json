{
  "private": true,
  "scripts": {
    "preinstall": "cd composite && yarn && cd ../web && yarn && cd ..",
    "db": "docker run --name hnme_postgres_1 --env POSTGRES_PASSWORD=postgrespassword --volume db_data:/var/lib/postgresql/data --publish 5432:5432 --rm --interactive --tty postgres:12",
    "dev": "dotenv -- node composite/service.js",
    "hasura-console": "hasura console --project hasura --endpoint http://localhost:3000 --admin-secret admin_secret",
    "build": "docker build --tag hnme_app .",
    "start-only": "dotenv -- docker run --name hnme_app_1 --env HASURA_GRAPHQL_DATABASE_URL --env HASURA_GRAPHQL_ADMIN_SECRET --env PORT=3000 --publish 3000:3000 --rm --interactive --tty hnme_app",
    "start": "yarn build && yarn start-only"
  },
  "dependencies": {},
  "devDependencies": {
    "dotenv-cli": "^3.2.0",
    "hasura-cli": "1.3.0-beta.4"
  }
}
